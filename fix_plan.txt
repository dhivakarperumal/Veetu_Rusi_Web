const pool = require('../config/db');
const crypto = require('crypto');
const https = require('https');
const { generateRoleId } = require('../utils/idGenerator');

// Rest of the file content will be preserved - we're just patching the specific functions

// PATCH: Replace lines 1155-1330 (createHomeChef function duplicated code problem)

// The issue: The function has duplicate file extraction code causing "const files already declared" error

// SOLUTION: Remove the old optional chaining code (lines 1160-1174) and keep the new one with proper variable naming

// OLD CODE TO REMOVE:
// const profilePhoto = files.profile_photo?.[0]?.filename || null;
// const coverBanner = files.cover_banner?.[0]?.filename || null;
// ... all the old const declarations ...
// const storagePhoto = files.storage_area_photo?.[0]?.filename || null;

// Then followed by duplicate:
// const files = req.files || {};  <-- THIS DUPLICATES THE FIRST files declaration!

// FIX: Use a helper function approach that avoids optional chaining issues
